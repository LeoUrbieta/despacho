<%= form_with(model: usuario_externo) do |form| %>
  <% if usuario_externo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(usuario_externo.errors.count, "error") %> prohibited this usuario_externo from being saved:</h2>

      <ul>
        <% usuario_externo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre_usuario, style: "display: block" %>
    <%= form.text_field :nombre_usuario %>
  </div>

  <div>
    <%= form.label :password_digest, style: "display: block" %>
    <%= form.text_field :password_digest %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<div class="row mb-3">
  <div class="offset-8 col-4">
    <% if logged_in? || usuario_externo_logged_in? %>
      <%=button_to("Terminar Sesion", logout_path, method: :delete) %>
    <% end %>
  </div>
</div>

<%unless usuario_externo_logged_in? %>
  <%if logged_in? && usuario_actual.admin? %>
    <hr>
    <div class="row">
      <div class="offset-4 col-2">
        <%= "Menú de Administrador" if usuario_actual.admin? %>
      </div>
    </div>
    <br>
    <div class="row">
      <div class="offset-4 col-2">
        <%=link_to "Usuarios Externos", usuario_externos_url %>
      </div>
      <div class="col-2">
        <%= link_to "Auditoria", auditoria_path%>
      </div>
    </div>
    <hr>
  <% end %>
<% end %>

<%if logged_in? %>
  <div class="row">
    <div class="offset-1 col-2">
      <%=link_to "Clientes", clientes_path%>
    </div>
    <div class="col-2">
      <%=link_to "Contabilidad", contabilidad_clientes_path%>
    </div>
    <div class="col-2">
      <%=link_to "Rep. Legales", replegales_path%>
    </div>
    <div class="col-2">
      <%=link_to "Casos", casos_path%>
    </div>
    <div class="col-2">
      <%=link_to "Peticiones", peticiones_path%>
    </div>
  </div>
<% end %>

<br>

<div class="row">
  <div class="offset-3 col-5">
    <% flash.each do |type, msg| %>
      <div class="alert alert-secondary">
        <%= msg %>
      </div>
    <% end %>
  </div>
</div>

<% if modelo.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(modelo.errors.count, "error", locale: :"es-MX") %></h2>
    <ul>
      <% modelo.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

